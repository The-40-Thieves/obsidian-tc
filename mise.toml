# Developer toolchain. Pinned to exact versions so two machines cannot silently disagree — a
# floating minor means "whatever was current when you installed", which is the one thing a pin
# exists to prevent.
#
# Each pin below matches something already authoritative elsewhere in the repo; none were chosen
# freely. Change one of these and change its counterpart in the same commit.
[tools]
# Matches BOTH .github/actions/setup-repo (bun-version default '1.4.0') and package.json's
# `packageManager: "bun@1.4.0"`. Bumped from 1.3.14 (THE-946): the root `bun audit` (509
# packages) hung on 1.3.14 until the client's five-minute timeout — 1.3.14's fetch idle timer
# did not cover the TLS handshake phase, so a stalled handshake to the npm advisory endpoint
# blocked forever instead of failing fast. 1.4.0 arms the idle timer in `on_open`, covering the
# handshake, and the same audit completes in ~17s.
bun = "1.4.0"

# Every package declares `engines.node: ">=24"`, so 24 is the floor, not the target. This pin is
# the exact version in use here and is deliberately ABOVE the floor: you develop on a newer
# runtime than the minimum you support, and CI validates on the floor itself.
#
# That split is intentional, and CI is now consistent about its half — every `node-version:` in
# .github is '24', including the setup-repo default. It was not: ci-security.yml and
# release-lag.yml ran on 22, BELOW the floor their own package.json declares.
#
# `perf:node-parity` is the job that actually executes under Node, so a dev/CI major mismatch
# would surface there first.
node = "26.5.0"

# Both Python services declare `requires-python = ">=3.10"` (services/bge-m3-service,
# services/docs-ingest), and ci-model-service.yml sets up `python-version: '3.11'`. 3.11 is what
# the model service is actually built and tested against, so it is what a developer should get —
# not whatever python3 the OS happens to ship (this machine defaults to 3.14, which no CI job
# validates and which much of the ML wheel ecosystem still lags on).
#
# Pinned to the minor, matching CI's specificity exactly. A patch pin here would be MORE specific
# than the thing it is meant to agree with, which just relocates the divergence.
python = "3.11"
